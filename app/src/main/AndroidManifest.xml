<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Benachrichtigungen -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Exakter Alarm -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />
    <!-- Wakelock -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <!-- Autostart nach Neustart -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Internet für Update-Check -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- APK-Installation -->
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Haptic Feedback -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TodoPro.Dark">

        <!-- Splash Screen -->
        <activity
            android:name=".SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.TodoPro.Splash"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Haupt-Activity -->
        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Statistik-Activity -->
        <activity
            android:name=".StatsActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- Archiv-Activity -->
        <activity
            android:name=".ArchiveActivity"
            android:exported="false"
            android:windowSoftInputMode="adjustResize" />

        <!-- BroadcastReceiver für Alarm-Benachrichtigungen -->
        <receiver
            android:name=".AlarmReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.todopro.app.ALARM_TRIGGER" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver für Neustart des Geräts -->
        <receiver
            android:name=".BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- BroadcastReceiver für tägliche Zusammenfassung -->
        <receiver
            android:name=".DailySummaryReceiver"
            android:exported="false"
            android:enabled="true" />

        <!-- FileProvider für APK-Installation -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.provider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

    </application>

</manifest>
